{
  "name": "filcdn-fwssdev",
  "license": "UNLICENSED",
  "scripts": {
    "build": "graph build",
    "codegen": "graph codegen",
    "create-local": "graph create --node http://localhost:8020/ filcdn-fwssdev",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ filcdn-fwssdev",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 filcdn-fwssdev",
    "remove-local": "graph remove --node http://localhost:8020/ filcdn-fwssdev",
    "select-calibnet": "cp subgraph-calibnet.yaml subgraph.yaml",
    "select-mainnet": "cp subgraph-mainnet.yaml subgraph.yaml",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.38.1"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
